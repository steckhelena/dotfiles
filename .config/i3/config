# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.


###################################################################
# Colors
###################################################################

# %%base16_template: i3##colors %%
## Base16 Rosé Pine Moon
# Author: Emilia Dunfelt &lt;sayhi@dunfelt.se&gt;
#
# You can use these variables anywhere in the i3 configuration file.

set $base00 #232136
set $base01 #2a273f
set $base02 #393552
set $base03 #59546d
set $base04 #817c9c
set $base05 #e0def4
set $base06 #f5f5f7
set $base07 #d9d7e1
set $base08 #ecebf0
set $base09 #eb6f92
set $base0A #f6c177
set $base0B #ea9a97
set $base0C #3e8fb0
set $base0D #9ccfd8
set $base0E #c4a7e7
set $base0F #b9b9bc

# %%base16_template_end%%

###################################################################
# Keyboard
###################################################################

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec "kitty --single-instance --listen-on unix:/tmp/mykitten"

# Fullscreen screenshot by pressing printscreen
bindsym Print exec xfce4-screenshooter -f -s $HOME/Downloads/screenshot_`date +%Y-%m-%d_%H:%M:%S`.png

# Screenshot menu by pressing $mod+printscreen 
bindsym $mod+Print exec xfce4-screenshooter

# Screenshot of selected area to clipboard
bindsym $mod+Shift+Control+4 exec "maim --hidecursor -s --format=png /dev/stdout | xclip -selection clipboard -t image/png -i"

# kill focused window
bindsym $mod+Shift+q kill

# lock screen
bindsym $mod+Shift+Control+l exec "~/.config/i3/scripts/lock.sh"

# System power menu
bindsym $mod+Shift+p exec "~/.config/i3/scripts/sysmenu.sh &"

# Audio output menu
bindsym $mod+Shift+m exec "~/.config/i3/scripts/sel_audio_dev.sh &"

# Network interface selection menu
bindsym $mod+Shift+n exec "~/.config/i3/scripts/sel_polybar_net_iface.sh &"

# start rofi (a program launcher)
bindsym $mod+space exec rofi -show combi -theme LaunchMenu

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus up
bindsym $mod+l focus down
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace and immediately swithces to that workspace
bindsym $mod+Shift+1 move container to workspace $ws1; workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2; workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3; workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4; workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5; workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6; workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7; workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8; workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9; workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10; workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# select themes
bindsym $mod+Shift+t exec "~/.config/i3/scripts/change_theme.sh $HOME"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

###################################################################
# Multimedia Keys
###################################################################
# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.config/i3/scripts/pactl_wrapper.sh 1 #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id ~/.config/i3/scripts/pactl_wrapper.sh -1 #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle # mute sound

# Media player controls
bindsym XF86AudioPlay exec --no-startup-id "playerctl play-pause"
bindsym XF86AudioNext exec --no-startup-id "playerctl next"
bindsym XF86AudioPrev exec --no-startup-id "playerctl previous"

###################################################################
# Mac specific keybinds
###################################################################
# Keyboard brightness
# bindcode 232 exec "~/.config/i3/scripts/backlight_control.sh -d 5"
# bindcode 233 exec "~/.config/i3/scripts/backlight_control.sh -i 5"

# Keyboard brightness
# bindcode 237 exec "kbdlight down 6"
# bindcode 238 exec "kbdlight up 6"

# Swipe left and right
# bindsym --whole-window button8 workspace prev
# bindsym --whole-window button9 workspace next

# Media settings
# bindcode 173 exec "playerctl previous"
# bindcode 172 exec "playerctl play-pause"
# bindcode 171 exec "playerctl next"
# bindcode 121 exec "amixer -q -M set Master toggle"
# bindcode 122 exec "amixer -q -M set Master 5%-"
# bindcode 123 exec "amixer -q -M set Master 5%+"

###################################################################
# System
###################################################################
# Enables numlock
exec_always --no-startup-id numlockx on

# Composite manager
exec --no-startup-id picom

# live reload of gtk themes
exec --no-startup-id xsettingsd

# Enables autolock after 15mins
exec --no-startup-id "xautolock -time 15 -locker '~/.config/i3/scripts/lock.sh'"

# Enables lock after sleep
exec --no-startup-id "xss-lock -- ~/.config/i3/scripts/lock.sh"

# System keyboard menu
bindsym $mod+Shift+d exec ~/.config/i3/scripts/kbdlayout.sh

# Disable nvidia power hungry card
# exec --no-startup-id "sudo ~/.config/i3/scripts/disable_gpu.sh"

# Network manger GUI
exec --no-startup-id nm-applet

# Bluetooth manager
exec --no-startup-id blueman-applet

# Enables redshift, to make display warmer
exec --no-startup-id redshift-gtk

###################################################################
# Windows
###################################################################
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $wsNull desktop

# i3 gaps
for_window [class="^.*"] border pixel 2
gaps inner 10
gaps outer 0
smart_gaps on
smart_borders on

# Property Name         Border  BG      Text    Indicator Child Border
client.focused          $base05 $base0D $base00 $base0D $base0C
client.focused_inactive $base01 $base01 $base05 $base03 $base01
client.unfocused        $base01 $base00 $base05 $base01 $base01
client.urgent           $base08 $base08 $base00 $base08 $base08
client.placeholder      $base00 $base00 $base05 $base00 $base00
client.background       $base07

# Enables desktop background manager
exec --no-startup-id ~/.config/i3/scripts/wallpaperslide.sh

# Enables popup window.
for_window [window_role="pop-up"] floating enable;border normal
for_window [window_role="task_dialog"] floating enable;border normal

# Start polybar
exec --no-startup-id polybar -r main

###################################################################
# Apps
###################################################################
#Open applications on specific workspaces
assign [class="^[Ff][Ii][Rr][Ee][Ff][Oo][Xx]$"] $ws1
for_window [class="^[Ff][Ii][Rr][Ee][Ff][Oo][Xx]$"] layout tabbed
for_window [class="Spotify"] move to workspace $ws2
for_window [class="Deezer"] move to workspace $ws2
assign [title="Deluge"] $ws3
assign [title="calibre"] $ws3
assign [class="Steam"] $ws4
for_window [class="Steam"] layout tabbed
assign [class="discord"] $ws5
assign [title="VLC media player"] $ws7
assign [class="streamlink-twitch-gui"] $ws8

# Workspace 1: Web
exec --no-startup-id "i3-msg 'workspace \"1\"; append_layout ~/.config/i3/workspaces/1.json'"
exec --no-startup-id firefox

# Workspace 2: Music
exec --no-startup-id deezer

# Workspace 3: Deluge
#exec --no-startup-id deluge-gtk

# Workspace 4: Steam
exec --no-startup-id "i3-msg 'workspace \"4\"; append_layout ~/.config/i3/workspaces/4.json'"

# Workspace 9: Config
exec --no-startup-id "i3-msg 'workspace \"9\"; append_layout ~/.config/i3/workspaces/9.json'"
exec --no-startup-id pavucontrol
exec --no-startup-id kitty --single-instance --listen-on unix:/tmp/mykitten --class="htop-terminal" -e htop

# Workspace 10: Terminal
exec --no-startup-id "i3-msg 'workspace \"10\"; append_layout ~/.config/i3/workspaces/10.json'"
exec --no-startup-id kitty --single-instance --listen-on unix:/tmp/mykitten --class="terminal-1"
exec --no-startup-id kitty --single-instance --listen-on unix:/tmp/mykitten --class="terminal-2" -e zsh -c ranger
exec --no-startup-id kitty --single-instance --listen-on unix:/tmp/mykitten --class="terminal-3"

# Default startup workspace
exec --no-startup-id i3-msg workspace 1

# Fix origin interface
for_window [instance="origin.exe"] floating enable
